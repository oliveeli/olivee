define(    [        "jquery",        "underscore",        "backbone",        "router",        './view/nav',        'bootstrap'    ],    function($, _, Backbone, Router, NavigationView) {	        var initialize = function(){            $('#menu').append($(new NavigationView().render().el));            Router.initialize();        };		  		          return {        	initialize: initialize        };    });